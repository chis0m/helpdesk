# Backend API origin (must match CORS FRONTEND_URL ↔ this app’s origin)
VITE_API_BASE_URL=http://localhost:8080

# Set to "true" to enable logger.debug in production-mode preview (vite preview)
# VITE_DEBUG=true
